# Reference template. The CLI loads the packaged _resources/validation_policy.yaml.
quaternion_tolerance: 1.0e-6
require_finite: true
missing_units_severity: error
required_tables_by_profile: {}
required_assets_by_solver:
  abaqus_cpfe: [mesh, orientation_map]
