# GH4169 工作簿接入与 CPU 评估验收

2026-09-07，本地 Windows，使用项目 `.venv`。起始 main 工作区干净，
HEAD 为 `a2e7141807404bf52cd71307d07610f71933811f`。
本轮只在本地增加 GH4169 案例与原表试样范围检查，未提交、推送或发布。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 新来源范围测试 + 现有构建测试 | 86 passed，9.41 s |
| GH4169 合成工作簿与真实 CPU 测试 | 9 passed，7.70 s |
| 完整离线回归 | 561 passed，2 skipped，34.92 s |
| wheel 与 sdist 构建 | 通过，wheel 从本次 sdist 构建 |
| twine check --strict | 两个产物通过 |
| 候选 wheel 在源码目录外安装验证 | 1 passed，4.13 s |
| 项目环境 pip check | 无依赖冲突 |
| 真实数据规范化 | 10 个 SamplePackage/HDF5 |
| 真实 CPU 优化 | 16 个 MLP，逐个保存训练记录与 checkpoint |
| 模型读回 | 与保存预测的最大绝对差为 0 |
| 训练集归一化、分组、来源、汇总指标复算 | 全部通过 |

全量测试跳过的是 opt-in Abaqus 和需要候选 wheel 的安装测试。后者在构建后
单独启用，使用独立目录和 `python -I` 检查安装包来源，完成 validate、独立
提取脚本帮助和训练集构建。没有执行真实 Abaqus 或重跑既有 tensile 模型。

新增实现先运行失败测试：来源范围测试初次为 9 failed、8 passed，失败来自
尚未支持的 target_specimen 配置及来源索引；已有拒绝路径测试能先通过。
案例初次 9 failed，原因是案例入口尚不存在。实现后全部通过。
案例测试实际运行合成工作簿解析和 CPU 优化，将 T1/T2 的目标分别增加 999 后
重新构建训练包；四种方法的全部预测均保持不变，检验测试目标没有参与训练、
归一化、checkpoint 或岭回归参数选择。合成夹具不包含公开数据的测量值。

针对性和全量测试使用最终实现代码。随后只补充结果文档，没有重新调参或训练。
构建产物仍使用项目现有版本 0.1.0，仅作本地安装验收。

## 数据与评估

DATA.xlsx 和六张金相 JPG 已从刷新后的公开文件 API 下载，全部与清单大小和
内容摘要一致。本轮普通下载由 302 跳转到公开存储并返回 200；此前 403 的
障碍未再出现。源数据使用 CC BY-NC 3.0，原文件及衍生数据、模型、图表均保留
在忽略目录。所有工作表和嵌入表头已检查，具体字段、异常和处理选择见
[案例说明](../../examples/gh4169_ultrasonic/README.md)。

输入固定为平均衰减系数和平均纵波声速，目标为金相平均晶粒直径。开发集八件
试样使用四对外层留出、三对内层验证，最终保留作者 T1/T2。现有 MLP 的
训练/验证/测试接口保持不变，至少两行的约束也保持不变。

| 方法 | 开发集外层 MAE / RMSE（n=8，μm） | 作者测试 MAE / RMSE（n=2，μm） |
| --- | ---: | ---: |
| 均值 | 44.9977 / 51.8441 | 20.6151 / 27.2159 |
| OLS | 9.1752 / 10.0018 | 5.5388 / 5.7084 |
| 岭回归 | 10.3065 / 11.4099 | 5.9598 / 5.9604 |
| MLP 集成 | 19.9168 / 26.5649 | 9.3780 / 9.3792 |

外层 MLP 成员训练样本数为四，最终成员为六；基线分别用六/八个开发试样
重新拟合。部分 MLP 的最佳 checkpoint 位于第一轮或最大训练轮，显示小样本
选择的不稳定。固定网络没有超过 OLS；这份同研究来源的十试样数据不足以
支持跨批次、设备条件的可靠预测结论。没有生成样本或复用作者拟合结果。

## 本地证据与检查范围

- `runs/public-data-screening/gh4169-20260907/raw/`：七个原始文件。
- 同目录 `refresh/`：公开文件清单、下载收据、请求头、全工作簿审查、嵌入表头，
  以及 `tdd-source-red/green.log`、`tdd-example-red/green.log`。
- `runs/gh4169-ultrasonic-20260907/normalized/`：原表读取配置、HDF5、默认构建配置、
  数据审查与来源清单。
- 同目录 `evaluation/`：16 组 NPZ/配置/训练收据/权重，完整精度预测 CSV、
  evaluation.json、逐试样误差 PNG/SVG。`REPORT.md` 整理全部原目标、预测和误差。
- 同目录 `acceptance.json`、`offline-tests.log`、`build.log`、`twine.log`、
  `installed-wheel.log`、`pip-check.log`：环境、实际验收和本地构建记录。

目标根来源检查默认仍按整文件限制。显式原表试样范围同时检查实体身份和
物理工作表/行，保留原文件来源。代码变更仅涉及训练构建配置/来源检查、案例
脚本和测试；MLP 优化核心、数据契约的三划分规则和既有 CuSn8Ni2 案例不变。
CI 配置已将新来源与案例测试加入 CPU job；本地新改动尚未运行远端 CI。

起始提交的远端 main 与本地 HEAD 已再次只读核对一致，
[该提交的既有 Actions 运行](https://github.com/17636365690/experiment-to-cpfe-pipeline/actions/runs/34097754102)
状态为 completed/success。这个远端结果属于起始提交，不代表本轮未提交的改动。
