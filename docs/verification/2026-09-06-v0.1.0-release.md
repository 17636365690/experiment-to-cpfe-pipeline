# v0.1.0 许可证与正式发行验证

本次首个正式 GitHub 发行采用 **Apache-2.0** 项目许可，公开拉伸加工材料采用
**CC-BY-4.0**。选择依据、源码归属、依赖及材料范围见
[许可核对](../licensing.md)和[第三方说明](../../THIRD_PARTY_NOTICES.md)。

发行范围包括许可文件、包元数据、变更说明、安装文档、版本标签、GitHub Release、
wheel 与 sdist。版本沿用包元数据和代码中的 `0.1.0`，标签为 `v0.1.0`。

## 当前本地验证

在项目 `.venv`、Windows、Python 3.12.10 下执行：

| 检查 | 当前结果 |
|---|---|
| `python -m pytest -q -ra` | **466 passed，2 skipped，25.90 秒** |
| `python -m pip check` | 依赖一致 |
| `python -m build --outdir <new-dist-directory>` | wheel 和 sdist 构建成功；wheel 从 sdist 构建 |
| `python -m twine check --strict <dist-files>` | 两份发行包均通过 |
| 设置 `EXP2CPFE_WHEEL_DIR` 后运行安装集成测试 | **1 passed，7.03 秒** |
| 源码目录外新建虚拟环境，安装 wheel 与核心依赖 | 安装、`pip check`、CLI 帮助和版本读取通过 |
| 独立环境的合成案例 | validate、build-inp、HDF5、NPZ、inspect 及 HDF5 读回通过 |
| TOML、CI YAML、文档链接目标与差异格式 | 通过 |

完整回归默认跳过真实 Abaqus 集成和需显式提供 wheel 的安装测试；安装测试随后
单独启用。独立环境第一次检查只复制了样本目录，缺少样本引用的 INP 模板。
保留该记录，补齐 `examples/` 与 `configs/` 目录结构后，完整合成流程通过。

## 发行包内容与来源

| 发行文件 | 大小 | 文件数 |
|---|---:|---:|
| `experiment_to_cpfe-0.1.0-py3-none-any.whl` | 112,106 字节 | 64 |
| `experiment_to_cpfe-0.1.0.tar.gz` | 93,268 字节 | 71，按普通文件计 |

两份归档中的 **56 个实现与资源文件**逐项比对当前 `src/experiment_to_cpfe`，
内容一致。检查包含 `py.typed`、默认校验策略、独立 ODB 提取器及共享场契约。
发行元数据记录 Python >=3.12、Apache-2.0、三个许可说明文件和 `pipeline` 入口。
sdist 的 README、变更说明及打包配置均与当前候选一致。

`LICENSE` 与 Apache 官方文本一致；`NOTICE` 和 `THIRD_PARTY_NOTICES.md`
同时进入 wheel 与 sdist。公开案例图、结构化结果和基准 INP 从版本化 GitHub
源码取得，其 CC-BY-4.0 适用文件、完整作者及加工过程已单独列明。

## 公开候选和历史

候选及发行归档按文件名、类型、大小、常见凭据和本机路径模式审查。候选中的
绝对路径命中是 `test_input_bundle.py` 中检查路径拒绝的合成输入。发行包仅包含
项目实现、资源与元数据。原始实验、ODB/CAE、模型权重、第三方 UMAT、私有
运行记录及机器配置继续保存在本地忽略目录。

发行前四个可达提交共 223 个唯一文件内容另行扫描。旧计划中的通用安装路径和
路径检查示例按历史说明保留，未发现凭据或受限原始文件。历史审计中“许可证待定”
描述对应当时的状态，本次许可核对记录了现已完成的选择。

## 科学案例与验证截止点

本次沿用[已完成的公开拉伸案例](2026-09-06-public-tensile-surrogate.md)：
H_08 标定、H_16 模型检查、H_18 最终实验评估，0–0.8% 工程应变的均匀标距段
小应变双线性弹塑性。12 个真实 FE 案例、84 个阶段、MLP 及对应误差保持原样。
本次核对源码、测试、基准 INP 和公开数值/图形与前一提交一致。

发行过程未重跑该组求解与训练。以上完整回归覆盖最终实现代码；随后只调整
README 图片链接及增加本报告和依赖许可说明，并完成最终包构建与安装验证。

## 下载与远端证据

[v0.1.0 Release](https://github.com/17636365690/experiment-to-cpfe-pipeline/releases/tag/v0.1.0)
提供正式 wheel、sdist、变更说明、最终提交的 Windows/Linux CI 链接及发布后下载核验结果。
CI 执行离线回归、构建、严格元数据检查和源码目录外 wheel 安装。
PyPI 发布作为后续独立事项。

```text
python -m pip install "https://github.com/17636365690/experiment-to-cpfe-pipeline/releases/download/v0.1.0/experiment_to_cpfe-0.1.0-py3-none-any.whl"
pipeline --help
```

本地发行工作记录位于忽略的 `runs/release-v0.1.0-20260906/`；最终发行包位于
其中的 `final-dist/`。
